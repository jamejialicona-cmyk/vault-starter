# LOG - append-only journal

## [2026-09-11] state | Demo vault initialised
Fixture created so `check_vault.py` runs against a real tree in CI. One entry is
enough: what matters is that the file exists, is discoverable, and stays under
its ceiling.

## [2026-09-22] ingest | Shared pattern: Quirks del Entorno
First file under `30-Loops/Patrones/`, the shared-pattern slot from
`VAULT-STARTER.md` section 6.5. Linked from the index and the Lint Loop, so it
is not an orphan.
