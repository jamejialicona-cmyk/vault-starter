# demo-vault

A minimal but complete Vault, used as a fixture. `scripts/test_check_vault.py`
audits it on every CI run, so the auditor is exercised against a real tree
rather than a mock.

It is also the smallest correct example of the layout described in
`VAULT-STARTER.md` section 3.
